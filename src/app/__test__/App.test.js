import { render, screen } from '@testing-library/react';
import App from '..//App';

test('renders app', () => {
  //wrender(<App />);
  //const appElement = screen.getByTestId("appId");
  //expect(appElement).toBeInTheDocument();
});
